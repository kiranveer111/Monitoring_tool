# frontend/.env
# This file defines environment variables for the frontend React application.

# Base URL for the backend API.
# When running locally, this will typically point to your Node.js backend.
# In a production environment, this should be the URL of your deployed backend.
REACT_APP_API_BASE_URL=http://localhost:3000/api
